<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		{# Run `composer require symfony/webpack-encore-bundle`
						           and uncomment the following Encore helpers to start using Symfony UX #}
		{% block stylesheets %}
			<link rel="stylesheet" href="https://bootswatch.com/5/yeti/bootstrap.min.css">
		{% endblock %}

		{% block javascripts %}
			<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
			<script src="{{ asset('assets/js/search.js') }}"></script>
		{% endblock %}
	</head>
	<body>
		<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
			<div class="container-fluid">
				<a class="navbar-brand" href="#">Navbar</a>
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>

				<div class="collapse navbar-collapse" id="navbarColor01">
					<ul class="navbar-nav me-auto">
						<li class="nav-item">
							<a class="nav-link active" href="{{path('home')}}">Home
								<span class="visually-hidden">(current)</span>
							</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{path('livre_index')}}">livre</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{path('user_index')}}">user</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{path('emprunt_index')}}">emprunt</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{path('rendre_livre')}}">rendre livre</a>
						</li>
						<li class="nav-item ">
							<a class="nav-link" href="{{path('app_register')}}">register</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{path('app_login')}}">login</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{path('app_logout')}}">logout</a>
						</li>
						<form class="d-flex" method="POST" name="search" action = "{{path('search_auteur')}}">
							<input class="form-control me-sm-2" type="text" placeholder="Search" name="auteur">
							<button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
						</form>
					</ul>

				</div>
			</div>
		</body>
	</body>
</html></nav>{% block body %}{% endblock %}</body></html>

